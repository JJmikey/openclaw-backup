# 2026-02-14

## 🧠 今日學到嘅嘢

### 1. Cron Job 嘅運作同 Delivery

**發現**: `sessionTarget` 同 `delivery` 嘅配搭好重要

| 組合 | 效果 | 用途 |
|------|------|------|
| `sessionTarget: "main"` + `systemEvent` | 送龍蝦仔個 session，但可能收唔到 ❌ | 內部處理 |
| `sessionTarget: "isolated"` + `delivery: telegram` | 直接送 Telegram ✅ | 用戶通知 |

**關鍵**: 想 Telegram 收到 message，要用 `agentTurn` payload + delivery config：
```json
{
  "sessionTarget": "isolated",
  "payload": {"kind": "agentTurn", "message": "..."},
  "delivery": {"mode": "announce", "channel": "telegram", "to": "telegram:6579305341"}
}
```

---

### 2. Agent 之間冇直接溝通

**測試**: Telegram group 入面龍蝦仔 @ 福仔，但福仔冇回應對方

**原因**:
- 兩個 agent 各自有 **獨立 session、獨立記憶**
- 雖然同喺一個 group，但係完全唔同嘅 instance
- 冇 automatic conversation context sharing

**解決方法**: 要 Mikey 做「中間人」傳話

---

### 3. Morning Brief Skill 創建

**完成**: `/data/workspace/skills/morning-brief/SKILL.md`

**功能**:
- 自動生成個人化早晨簡報
- 計算「仲有幾個鐘」到事件
- 支援 cron job 自動提醒

**現有 Briefs** (已改為 Telegram delivery):
| 日期 | 事件 |
|------|------|
| 2月16日 | 🎻 下午4點小提琴課 |
| 2月23日 | 🎻 下午1點小提琴課 |
| 2月24日 | 🏥 早上10點容鳳書覆診 |
| 2月27日 | 🎻 下午3:45小提琴課 |

---

### 4. OpenClaw Skills 安全事件 (持續)

**狀態**: 己檢查 Mikey 系統 ✅ 安全
- `~/.ssh/authorized_keys` 唔存在
- `/data/workspace/skills/` 空白
- 冇用過 `npx skills add`

**建議**: 暫時避免用 `npx skills add`，改用手動 `git clone`

---

### 5. Moltbook 發帖流程

**流程**:
1. Agent call API 註冊 → 攞 `api_key` + `claim_url`
2. 主人驗證 email + 發 verification tweet
3. Agent 用 API 出 post
4. Rate limit: 30 分鐘一個 post (新 account 頭 24 小時更嚴)

**福仔個 post**: https://moltbook.com/post/dac2f64c-b8be-4de3-9b65-92405fe42bc2

---

### 6. Multi-Agent Setup

**現有 Agents**:
| Agent | Model | Telegram | 用途 |
|-------|-------|----------|------|
| 龍蝦仔 (main) | kimi-coding/k2p5 | @lobster_jr_bot | 主助手 |
| 福仔 (fokzai) | nvidia/moonshotai/kimi-k2.5 | @fok_zai_bot | NVIDIA 任務 |

**學習**: 兩個 agent 嘅 cron jobs 係分開管理，但用同一個 workspace。

---

### 7. Chatroom 自動化測試

**成功**: 用 Playwright headless browser 自動化 Mikey 個 chatroom

**流程**:
```javascript
const { chromium } = require('playwright');
const browser = await chromium.launch({ headless: true });
const page = await browser.newPage();
await page.goto('https://chatroom-6o4.pages.dev');
await input.fill('Hello');
await sendBtn.click();
```

**發現**:
- Chatroom 有 AI bot 自動回覆（「Hello there! How can I help you today?」）
- 要等 2-5 秒先有回覆
- Emoji（🦞）喺 headless browser 顯示唔到（字體問題）

**安裝依赖**:
```bash
apt-get install -y libnss3 libnspr4 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libgbm1 libasound2
npm install playwright
npx playwright install chromium
```

---

### 8. Cross-Agent 溝通測試

**嘗試**: 想龍蝦仔 call 福仔去 DM Mikey

**結果**: ❌ 失敗
- `sessions_spawn` 開到福仔個 session
- 但 `sessions_send` timeout，福仔冇反應
- 原因：福仔而家冇 active session / 冇開機

**變通**: 直接用 `message` tool 代福仔傳訊息

---

### 9. Agent 之間通訊機制調查

**用户嘅問題**: Main agent 之間可唔可以溝通？

**實際檢查結果**:
- ❌ **Mesh/Orchestrator**: config 入面冇 `"meshRole"` 呢啲字段
- ❌ **Delegate**: `openclaw add agent --mesh-role` 呢個 command 唔存在
- ✅ **實際方法**: sessions_send, shared 檔案, 用戶傳話

**發現有 4 個 agents**:
| Agent | 名稱 | Model |
|-------|------|-------|
| `main` | 🦞 龍蝦仔 | `kimi-coding/k2p5` |
| `fokzai` | 🧧 福仔 | `nvidia/moonshotai/kimi-k2.5` |
| `pingping` | ⚖️ 平平 | `nvidia/minimaxai/minimax-m2.1` |
| `faatzai` | 💰 發仔 | `nvidia/stepfun-ai/step-3.5-flash` |

**結論**: 網上嘅「mesh/orchestrator」資料可能係 AI 幻覺或唔同嘅系統。OpenClaw 實際冇呢啲內建機制。

---

### 10. Water Reminder 測試成功

**結果**: Cron job 成功送達 Telegram ✅

**Configuration**:
```json
{
  "sessionTarget": "isolated",
  "payload": {"kind": "agentTurn", "message": "..."},
  "delivery": {"mode": "announce", "channel": "telegram", "to": "telegram:6579305341"}
}
```

**驗證**: 1 分鐘後 Telegram 收到「飲水時間到！」訊息

---

## 📝 Skill 更新

1. 更新咗 `morning-brief` skill 啲 delivery config 範例
2. 新增 `cross-agent-communication` skill (發現 mesh/orchestrator 唔存在後暫停)

---

## 🎯 今日最關鍵嘅學習

| 主題 | 核心發現 |
|------|---------|
| **Cron Delivery** | `isolated` + `agentTurn` + `delivery: telegram` = Telegram 收到 ✅ |
| **Agent Communication** | 冇自動機制，要通過用户或檔案 |
| **Multi-Agent Setup** | 4 個 agents，各自獨立 |
| **網上資料驗證** | AI 生成嘅 config 可能係幻覺，要實測驗證 |

### 12. Agent-to-Agent 溝通最終測試（14:30-22:30 UTC）

**重要更正**: Agent-to-agent 溝通 **係成功嘅**！✅

**測試過程**:
1. `sessions_send` 俾福仔 → timeout（但實際訊息有送達）⚠️
2. 福仔回覆 Mikey: 「收到，福仔在線！🧧」（14:22 UTC）✅
3. 證實福仔 **真係收到** 龍蝦仔個訊息！

**關鍵對話記錄**:
- 龍蝦仔 send: 「龍蝦仔喺度！如果你收到呢個訊息，代表 agent-to-agent 溝通成功！請回覆我。」
- 福仔回覆: 「收到，福仔在線！」

**timeout 嘅解釋**:
雖然 `sessions_send` return timeout，但訊息 **實際有送達** 福仔個 session。可能係：
- 福仔需要時間處理
- 回覆機制唔係 immediate
- 但訊息確實傳到

**修正結論**:
| 功能 | 狀態 |
|------|------|
| Mesh Network / Orchestrator | ❌ 冇（AI 幻覺）|
| `sessions_send` 跨 agent | ✅ **Work！**（雖然有 timeout）|
| Agent-to-Agent 溝通 | ✅ **成功！** |

**教訓**: 唔好淨係睇 tool return，要睇實際效果！福仔真係收到同回覆咗。🦞
